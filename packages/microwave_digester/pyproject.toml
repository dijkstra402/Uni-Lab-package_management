[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "microwave_digester"
version = "0.1.0"
description = "Uni-Lab-OS 微波消解仪标准设备类模板 (microwave_digester device class template)"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
dependencies = []

[project.optional-dependencies]
dev = ["unilabos"]

[tool.setuptools.packages.find]
include = ["microwave_digester*"]
