[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "overhead_stirrer"
version = "0.1.0"
description = "Uni-Lab-OS 机械搅拌器标准设备类模板 (overhead_stirrer device class template)"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
dependencies = []

[project.optional-dependencies]
dev = ["unilabos"]

[tool.setuptools.packages.find]
include = ["overhead_stirrer*"]
