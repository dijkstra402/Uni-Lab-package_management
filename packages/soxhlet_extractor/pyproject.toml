[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "soxhlet_extractor"
version = "0.1.0"
description = "Uni-Lab-OS 索氏提取仪标准设备类模板 (soxhlet_extractor device class template)"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
dependencies = []

[project.optional-dependencies]
dev = ["unilabos"]

[tool.setuptools.packages.find]
include = ["soxhlet_extractor*"]
