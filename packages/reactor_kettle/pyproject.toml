[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "reactor_kettle"
version = "0.1.0"
description = "Uni-Lab-OS 反应釜标准设备类模板 (reactor_kettle device class template)"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
dependencies = []

[project.optional-dependencies]
dev = ["unilabos"]

[tool.setuptools.packages.find]
include = ["reactor_kettle*"]
